# Documentation Reconciliation

Corrigir docs para refletir a realidade:
- React Query: futuro, não atual.
- React Hook Form + Zod: futuro.
- /api/v1: não implementado; não migrar agora.
- Prisma ORM: schema derivado; SQL/pg é runtime atual.
- Broker App: futuro.
- shared/config/validation: não existe; não criar agora.
- CI/CD: refletir estado final.
- backup/observability: distinguir local de staging real.

Nunca alterar código só para corresponder a docs antigas.
