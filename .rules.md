# Rules Bridge

`AGENTS.md` is the official global source of truth for AI agent rules in this
repository.

This file is kept only as a compatibility bridge for tools that look for
`.rules.md`.

Do not duplicate global rules here. Read and follow `AGENTS.md`.
